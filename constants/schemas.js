import { schema } from "normalizr";

export const cart = new schema.Entity("cart");
export const auth = new schema.Entity("auth");
export const user = new schema.Entity("users");
export const settings = new schema.Entity("settings");

